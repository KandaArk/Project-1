Bukreev Andrey
Bukreev Andrey


#include<stdio.h> 
#include<stdlib.h> 
include<math.h>    
void main()  
{
	system("Chcp 1251");
	const double pi = 3.14;
	double R,S; 
	printf("ведите радиус круга ");
	scanf("%lf", &R); 
	S = pi*pow(R,2); 
	printf("площадь круга ровна %.2lf", S); 
} 
#include<stdio.h> // подключение библиотеки ввода и вывода
#include<stdlib.h> // подключение библиотеки для использования кирилицы
#include<math.h>    //подключение математической библиотеки
/*Предусловие: Ввод радиуса с консоли
Постусловие : выводт площади круга*/
void main()  //заголовок функции
{//начало функции
	system("Chcp 1251");// меняет латиницу кирилицу
	const double pi = 3.14;// ввод постоянной
	double R,S; //ввод переменых
	printf("ведите радиус круга ");//вывод надписи в командную строку
	scanf("%lf", &R); // ввод значения переменй
	S = pi*pow(R,2); //уровнение
	printf("площадь круга ровна %.2lf", S); //вывод ответа
} //конец функции

